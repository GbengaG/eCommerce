/// Enums aare used for comparisms, they are not functions exactly
///
enum TextSizes { small, medium, large}

enum OrderStatus { processing, shipped, delivered}

enum PaymentMethods { paypal, goooglePay, applePay, visa, masterCard, creditCard, paystack, razorPay, paytm}